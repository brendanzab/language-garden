(cram
  (package compile-arithcond)
  (deps %{bin:compile-arithcond}))

(tests
  (names Properties)
  (package compile-arithcond)
  (libraries
    compile-arithcond
    alcotest
    qcheck
    qcheck-core
    qcheck-alcotest))
