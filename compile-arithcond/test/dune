(cram
  (package compile-arithcond)
  (deps %{bin:arithcond}))

(tests
  (names Properties)
  (package compile-arithcond)
  (libraries
    compile-arithcond
    alcotest
    qcheck
    qcheck-core
    qcheck-alcotest))
