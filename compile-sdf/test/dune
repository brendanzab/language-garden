(cram
  (deps %{bin:compile-sdf}))

(rule
  (action (with-stdout-to scene.glsl.out
    (run %{bin:compile-sdf} compile))))

(rule (alias runtest)
  (action (diff scene.glsl scene.glsl.out)))

(rule
  (action (with-stdout-to scene.png.out
    (pipe-stdout
      (run %{bin:compile-sdf} render)
      (run pnmtopng)))))

(rule (alias runtest)
  (action (cmp scene.png scene.png.out)))
