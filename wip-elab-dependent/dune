(executable
  (name main)
  (public_name wip-elab-dependent)
  (package wip-elab-dependent)
  (libraries pp))

(menhir
  (modules SurfaceParser)
  (flags --explain --strict))

(ocamllex SurfaceLexer)
