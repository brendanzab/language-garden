fun fact(n : I32) : I32 :=
  let cond := #i32-eq(n, 0);
  join j (p : I32) := p;
  if cond then
    jump j 1
  else
    let x := #i32-sub(n, 1);
    let x_1 := fact(x);
    let r := #i32-mul(n, x_1);
    jump j r;

val test-fact : I32 := fact(5);
