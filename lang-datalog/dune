(executable
  (name Main)
  (public_name datalog)
  (package lang-datalog))

(menhir
  (modules Parser)
  (flags --explain --strict))

(ocamllex Lexer)

(mdx
  (package lang-datalog)
  (deps %{bin:datalog}))
