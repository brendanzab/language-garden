(executable
  (name main)
  (public_name old-elab-dependent)
  (package old-elab-dependent)
  (libraries pp))

(menhir
  (modules SurfaceParser)
  (flags --explain --strict))

(ocamllex SurfaceLexer)
