; @generated by `dune build --auto-promote`


; dependent-record.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to dependent-record.mltt.elab.stdout.exp
      (with-stdin-from dependent-record.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to dependent-record.mltt.norm.stdout.exp
      (with-stdin-from dependent-record.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff dependent-record.mltt.elab.stdout dependent-record.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff dependent-record.mltt.norm.stdout dependent-record.mltt.norm.stdout.exp)))


; identity.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to identity.mltt.elab.stdout.exp
      (with-stdin-from identity.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to identity.mltt.norm.stdout.exp
      (with-stdin-from identity.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff identity.mltt.elab.stdout identity.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff identity.mltt.norm.stdout identity.mltt.norm.stdout.exp)))


; let.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to let.mltt.elab.stdout.exp
      (with-stdin-from let.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to let.mltt.norm.stdout.exp
      (with-stdin-from let.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff let.mltt.elab.stdout let.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff let.mltt.norm.stdout let.mltt.norm.stdout.exp)))


; monoid.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to monoid.mltt.elab.stdout.exp
      (with-stdin-from monoid.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to monoid.mltt.norm.stdout.exp
      (with-stdin-from monoid.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff monoid.mltt.elab.stdout monoid.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff monoid.mltt.norm.stdout monoid.mltt.norm.stdout.exp)))


; type.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to type.mltt.elab.stdout.exp
      (with-stdin-from type.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to type.mltt.norm.stdout.exp
      (with-stdin-from type.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff type.mltt.elab.stdout type.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff type.mltt.norm.stdout type.mltt.norm.stdout.exp)))
