; @generated by `dune build --auto-promote`


; dependent-record.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to dependent-record.mltt.elab.stdout.exp
      (with-stdin-from dependent-record.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to dependent-record.mltt.norm.stdout.exp
      (with-stdin-from dependent-record.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff dependent-record.mltt.elab.stdout dependent-record.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff dependent-record.mltt.norm.stdout dependent-record.mltt.norm.stdout.exp)))


; identity.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to identity.mltt.elab.stdout.exp
      (with-stdin-from identity.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to identity.mltt.norm.stdout.exp
      (with-stdin-from identity.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff identity.mltt.elab.stdout identity.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff identity.mltt.norm.stdout identity.mltt.norm.stdout.exp)))


; let.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to let.mltt.elab.stdout.exp
      (with-stdin-from let.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to let.mltt.norm.stdout.exp
      (with-stdin-from let.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff let.mltt.elab.stdout let.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff let.mltt.norm.stdout let.mltt.norm.stdout.exp)))


; monoid.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to monoid.mltt.elab.stdout.exp
      (with-stdin-from monoid.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to monoid.mltt.norm.stdout.exp
      (with-stdin-from monoid.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff monoid.mltt.elab.stdout monoid.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff monoid.mltt.norm.stdout monoid.mltt.norm.stdout.exp)))


; prelude.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to prelude.mltt.elab.stdout.exp
      (with-stdin-from prelude.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to prelude.mltt.norm.stdout.exp
      (with-stdin-from prelude.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff prelude.mltt.elab.stdout prelude.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff prelude.mltt.norm.stdout prelude.mltt.norm.stdout.exp)))


; record-proj-1.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to record-proj-1.mltt.elab.stdout.exp
      (with-stdin-from record-proj-1.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to record-proj-1.mltt.norm.stdout.exp
      (with-stdin-from record-proj-1.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff record-proj-1.mltt.elab.stdout record-proj-1.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff record-proj-1.mltt.norm.stdout record-proj-1.mltt.norm.stdout.exp)))


; record-proj-2.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to record-proj-2.mltt.elab.stdout.exp
      (with-stdin-from record-proj-2.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to record-proj-2.mltt.norm.stdout.exp
      (with-stdin-from record-proj-2.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff record-proj-2.mltt.elab.stdout record-proj-2.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff record-proj-2.mltt.norm.stdout record-proj-2.mltt.norm.stdout.exp)))


; type.mltt

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to type.mltt.elab.stdout.exp
      (with-stdin-from type.mltt (run mltt elab)))))

(rule
  (deps ../main.exe)
  (action
    (with-stdout-to type.mltt.norm.stdout.exp
      (with-stdin-from type.mltt (run mltt norm)))))

(rule
  (alias runtest)
  (action
    (diff type.mltt.elab.stdout type.mltt.elab.stdout.exp)))

(rule
  (alias runtest)
  (action
    (diff type.mltt.norm.stdout type.mltt.norm.stdout.exp)))
