(cram
  (package compile-arith)
  (deps %{bin:compile-arith}))

(tests
  (names Properties)
  (package compile-arith)
  (libraries
    compile-arith
    alcotest
    qcheck
    qcheck-core
    qcheck-alcotest))
